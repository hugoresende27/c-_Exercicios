#include <iostream>	
		
using namespace std;


int main()								
{
    bool bissexto = true;

    if (bissexto)
    {
        printf ("%d",bissexto);
        cout << (bissexto);
    }
    else
    {
        printf ("%d",bissexto);
        cout<< (bissexto);
    }
    cout<<endl;
    system("pause");				
	return 0;	
}